# Direct Business — full handoff brief (2026-08-09)

Written at the end of a long session. Read this **and** `CLAUDE.md` before touching anything.
Everything here is verified against the **live** database and the repo, not the test harness.

---

## LATEST — 2026-08-10 · Ownership (real users own leads/clients/proposals) — v56

Made records belong to **real people**, the foundation every leading CRM stands on — so each rep
gets "my pipeline / my clients / my proposals" and a manager sees who's accountable.

- **`team_directory` view** (DB): `SELECT id,email,full_name,role,active FROM app_users WHERE active`,
  granted to `authenticated`. RLS on `app_users` only lets a user read their own row (or admins read
  all); the view (owner-rights, not security_invoker) lets **every signed-in user** read the roster
  for the owner dropdowns, without loosening `app_users` security. 10 active users.
- **v56 layer** (`index.html`): loads the roster into `window.__TEAM` and overrides `teamList()` so
  every "assigned to / account manager / owner" dropdown offers **real people**, not free text
  (falls back to the old list if the view is unreachable, e.g. the harness). Adds **`meName()`** —
  the reliable current-user display name from `DB.settings.currentUser` (the global `me` is the auth
  session **object** after login, hence the app's many `typeof me==='function'` guards; `meName()`
  is the correct source and the fix for the Mine filters).
- **One-click "Mine"** (👤 / «خاص بي») on **Leads** (`leadFilter.mine`), **Clients** (`clToggleMine`
  reuses the existing owner filter) and **Proposals** (`window.offerMine`) — filters to
  owner === the signed-in user. New leads/offers now default their owner to the creator.
- Verified: `test-ownership` 8/8 (buttons present EN+AR, each Mine filters to exactly the signed-in
  user's records, counts match a DB-computed expectation), full regression green, deep-sweep 0
  page-errors, screenshots `OWN-EN/AR-*`.

---

## LATEST — 2026-08-10 · Offer Builder → full "proposal store" (Customer-360)

Turned the flight-only Offer Builder into a place to store **every kind of proposal** — a Samsung-style
corporate-travel tender, a technical/financial bid, a business-solution write-up, a training offer, a
plain price offer — not just a flight quote. Each proposal now links to the client record and can carry a
**link to the actual proposal document** (Drive/PDF), and can be **promoted to a project**. Design/identity
of the proposal document itself is deliberately a **later** pass (user's call).

**What changed in `index.html` (all self-contained, EN+AR, 0 page-errors):**
- **Proposal fields on every offer** (`newOffer`): `proposalType` (Price offer · Technical proposal ·
  Financial bid · Business solution · Training · Tender · Travel — flights · Other), `docUrl` (link to the
  file), `scope` (summary), `value` (deal size), `promotedToProject`. `PROPOSAL_TYPES` + `PROPOSAL_TYPES_AR`.
- **Proposal header card** at the top of the offer editor: Type dropdown, linked-Client dropdown (of real
  clients), Value, Valid until, Scope/summary, **Document link + Open↗**, and a **Promote to project** button
  (turns into a "✓ Promoted to project" tag once used). `o_setClient` sets `linkedLeadId`+`client`;
  `o_promoteProject` pushes a row into `DB.projects` and marks the offer. The existing detailed flight-quote
  form stays **below** the header for when a proposal *is* a flight quote.
- **Proposals list** (`drawOffers`): new **Type** and **Value** columns; 📎 marks a proposal with a document,
  🏗 marks one promoted to a project; header + empty state say "proposal"/"عرض"; "+ New proposal".
- **v55 layer — Proposals on the client/lead detail card** (Customer-360): a "Proposals (N)" section listing
  that client's proposals (type · subject · value), with its own "+ New proposal" that opens the editor
  pre-linked to the client. Gated + idempotent like the other detail-card layers (`.v55-proposals`).

**Seeded (assumption data, in `app_state` blob):** 3 proposals — Rawabi *Tender* 1.25M (Drive doc, linked),
Falcon *Business solution* 320K, Bright Horizons *Price offer* 96K (promoted → project) — + 4 operational
requests (Rawabi/Tamimi/Bright/Falcon) + 1 project. All tie to real assumption clients (checked by SQL).
`scripts/qa/mock-seed.mjs` mirrors them so the harness shows the same. Drive folder that informed the type
list: proposals/offers folder `1pG4Sgp8Jo7zUqNz5DuMFDkW6X18XBcqR` (price offers, technical/financial bids,
tenders, business solutions, training).

**Verified:** `test-proposals` 9/9 (list, Type/Value columns, Type + linked-Client dropdowns, Document link +
Scope + Promote present, client-card Proposals section lists the linked proposal, 4 seeded requests on the
Ops board, Arabic header localized) · full regression green (test-final 0 err, rollup 14/14, lineitems 7/7,
services-conv 7/7) · full-app deep-sweep **0 page-errors** EN+AR · screenshots `PROP-EN/AR-*`.

---

## LATEST — 2026-08-10 (late night) · assumptions & testing phase — clean slate + full-loop build

**Wiped + reseeded (user-directed "assumptions & testing phase").** Backups first:
`*_prewipe_20260810` (businesses 1035 / contacts 335 / finance_invoices 176 / finance_client_links 14 /
app_state 1) + the earlier `_20260810` snapshots. Restore = copy back by id / re-insert from these.

Live DB now = **10 assumption leads → 3 converted clients → 12 per-service invoices**:
- 10 leads across all funnels + stages (New×3/Contacted×3/Qualified×2/Proposal×2), each with bilingual
  name, funnel_details, contacts, dated activities, owner, next action. legacy_ids `L_nadeem, L_falcon,
  L_rawabi, L_tamimi, L_mosport, L_alyusr, L_saned, L_bright, L_qimam, L_durrat`.
- 3 clients (Rawabi=Direct 101, Tamimi=102, Bright Horizons=103) with the full handover payload
  (legal name, entity/customer type, CR·VAT, payment mode·cycle, credit limit·terms, agreement ref·date,
  POC, contract scope, win reason) in both `raw` and the real columns.
- Finance: 12 service-line rows (`source_batch='assumption seed'`, grouping tag in `notes` e.g. `inv:RAW-JUL`),
  services Flights/Hotels/Transport/Visa/Umrah, linked to the 3 clients → reports roll up by client + service.

**Code shipped this round (all verified EN+AR, 0 page-errors):**
- **Service catalogue** `SVC_CATALOG` + `window.svcLabel(k)` (EN→AR for Flights/Hotels/Visa/Transfers/
  Car rental/Insurance/Tours/MICE/Packages/Umrah/Hajj/Courses/Training/Wallet/Support/eSIM/Mixed/Other).
  Wired into: Report Builder (`dimVal` service_type), by-service card (v32), Ledger service filter
  (Arabic labels, English values kept) + service cell. Added Umrah/Hajj to the v33 service-fit map.
- **Ledger fully Arabic**: filters (quarters/months/services/clients/status), column headers, row badges,
  empty/cap messages, summary line.
- **`appToRow` now persists client columns** (legal_name, cr_vat, entity_type, payment_terms, credit_limit,
  contract_scope, converted_date) — a lead converted in-app writes the same shape as the SQL seed.
- **v40 handover modal enriched**: added Legal name, Customer type (dropdown), Payment mode (dropdown),
  Billing cycle (dropdown), CR/VAT, Credit limit, Agreement status (dropdown) — the data Direct needs
  to create the client. 12 fields, 4 dropdowns, bilingual.

**Invoice line-items (DONE).** `finance_invoices` now allows several service lines under ONE invoice
number: added `line_no`, dropped the unique on `invoice_no`, added UNIQUE `(invoice_no, line_no)`. Assumption
finance reseeded as 5 real invoices / 12 service lines (DP-2001 Flights+Hotels, DP-2002 Flights+Transport+Visa,
DP-2005 Flights+Hotels+Transport+Umrah, …). The Ledger invoice modal (`finRow`) now shows the invoice's
full service-line breakdown + invoice total; `finDelInv`/`finRestoreInv` soft-delete/restore the whole
invoice. Counts corrected for the line model: report-builder `_count` relabeled **"Service lines"**, while
the **Overview "Invoices" KPI**, the **finTabs summary**, and the **by-service card** now count DISTINCT
invoices. Converted 3 more leads → **6 clients / 4 leads** (Direct IDs 101–106).

**Landmine (all clean).** Real data: revenue=total−wallet ✓, profit=revenue−cost ✓, 0 null/dup line keys,
5 invoices/12 lines, revenue 839,000 = 100% attributed to clients, 0 orphan links, 0 links-to-non-client,
0 client-flag mismatch, 6/6 clients carry a Direct ID, 4/4 leads a funnel, 0 dup Direct IDs, RLS on all 3
tables. Code: `test-lineitems` 7/7 (modal shows both lines + total, KPI=distinct, "Service lines" label,
grand exact) + full suite green, 0 page-errors.

**Jargon sweep (DONE).** Automated visible-text sweep of all 4 pages in Arabic (`scripts/qa` + scratchpad
`sweep-jargon.mjs`, visibility-filtered so hidden dev cards don't false-positive). Fixed: the Leads table
"Funnel" column + the lead/client detail-card "Funnel" fact were showing the record's **source** ("Assumption
seed"), not the real funnel — now show the funnel name (EN name in EN, `name_ar` in AR via a new
`o.funnelNameAr`); "Priority" header was untranslated → added `'Priority':'الأولوية'` to the V27_AR relabel
map; the seven real funnel names (Travel Trade, Partners & Tenders, Website Form — B2B/Entities, Outreach &
Network, Past Invoices) added to V27_AR so the "All funnels" dropdown + chips localize. Confirmed the Settings
dev/test-harness cards stay hidden (they were `textContent` false-positives). Residual sweep hits are
intentional (brand "Direct Travel", language codes EN/AR). Full regression green, 0 page-errors; screenshots
`scripts/qa/mock-seed.mjs` mirrors the seed (6 clients / 4 leads / multi-line finance).

Import path stays one-line-per-invoice (real Direct export shape).

---

## LATEST — 2026-08-10 (night) · real-data enrichment from Drive + operational rehearsal

Backed up, enriched from Google Drive, rehearsed the pipeline on live data. Backups (admin-only,
2026-08-10): `businesses_snapshot_20260810` (1035), `contacts_snapshot_20260810` (335),
`finance_invoices_snapshot_20260810` (176), `finance_client_links_snapshot_20260810` (14).
Undo any edit below = copy the column back from these by id.

**Drive find — the client-creation schema + the Direct client IDs.** The "Direct Corporate – B2B
Admin Panel" export (`corporate-clients` xlsx `1W6B_9x-ra16NRE3Ruo7Ha3cJXVtChev0`) is the client
master: ID · Legal Name (EN+AR) · Trading Name · Customer Type · Payment Mode (Prepaid/Postpaid/
Tender) · Billing Cycle · CR/VAT (ID type+number) · contact · Credit Limit/Term · Block-on-overdue ·
Pricing Setting. Real client → Direct ID: **Takamol=7, Maaden(Altadin…Maaden)=6, The Ultimates=5,
Booking and ticket agency=3, Riyadh Chamber=23, Benchmark=8, Kayan=9, Sidra=47, MIRAGE=19,
National Electronic Systems=27, alnahla=46**; DUPLICATE Direct IDs (need user's canonical pick):
**Directorate 26/95, Maaal 4/24, MDD 1/2, Abdel Hadi 17/20**. Companion `corporate-invoices` xlsx
(`1ts7vrIgUADVxFvPIjlHEFu3bL_FhqZpn`) is invoice-level (Invoice# · Amount · Client · type · dates) and
matches app finance_invoices. Per-item detail is in `corporate_transactions.html`/`corporate_cogs_report.html`
(3–4MB, not yet ingested).

**⚠ Two ID systems — CONFIRM WITH USER.** Corporate-portal IDs are small (1–95); the per-customer
Direct-**Payments** invoice exports are numbered 5466–5507 (e.g. 5504 = Al-Qahtani/Sinopec). "The
Direct client ID" could be either. Applied the **corporate-portal (1–95)** system because that's the
client-creation page the user shared. If the real key is the Payments customer number, swap.

**Applied on live data (reversible via snapshot):**
- direct_client_id: Takamol=**7**, Maaden=**6**, The Ultimates=**5** (3 of 46 clients now carry a Direct ID; was 0).
- finance links added (flagged group → existing app client): "Ma'aden (merged)"→b_maaden (126,589 SAR),
  "Abdel Hadi…Al-Qahtani (merged)"→b_qahtani (30,518 SAR). Now **8 of 14** finance groups link to a client.
- Revenue attributed to real clients: **11,594,023 of 13,039,327 SAR (89%)**.

**Left for the user (didn't guess):** canonical Direct ID for the 4 duplicate-ID clients; the 4 remaining
flagged finance groups have NO client record in the app (MDD, Riyadh Chamber, STUDY ABROAD COMMISSION,
Special Forces for Security) — make them clients or leave; app-side DUPLICATE client rows (two "Booking…",
two "Directorate of Public Security" — one stuck at stage `new`, English "Kayan Orphans Charity" + Arabic
"جمعية كيان للايتام"); and ~35 "Old Customers" clients whose Direct IDs aren't in the corporate export
(they're in the 5466–5507 per-customer files or an Old-Customers export the user will provide).

---

## 2026-08-10 (evening, +2) · Finance reports rolled up by the LINKED (canonical) client

(shipped commit 6834dd0) Report Builder + Overview Top-clients now group by the linked client via
`finCanon()` (business_id→name through __bizUuid), not the raw client_group; Ledger gains a canonical
drill filter. Grand totals unchanged. Also fixed `isArF()` (was always false → Finance page rendered
English in Arabic mode). Verified test-rollup 14/14 + full suite, 0 page-errors.

---

## LATEST — 2026-08-10 (evening) · the four-page spine: link clients ↔ finance by the Direct client ID

The focus is now the four go-live pages (Leads · Clients · Finance · Settings/users) linked into one
system. The link key is the **Direct client ID** (per `DIRECT_SYSTEMS_MAP.md`). Built the spine:

**Schema (Supabase, applied):**
- `businesses.direct_client_id text` — first-class external reference to the client in Direct Payments.
  Was previously only inside the `raw` JSON; now a real column. `rowToApp`/`appToRow` map it both ways.
- `finance_client_links` — one row per finance `client_group` → a real client (`business_id` uuid) or
  `is_client=false` (individuals bucket). RLS mirrors finance_invoices (read = any app user, write =
  admin/manager). Seeded: **8 of 14 auto-resolved** (6 exact-name clients + 2 INDIVIDUALS buckets);
  6 "(merged)"/other groups flagged `needs mapping`.

**Why:** finance was matched to clients by **name** (`client_group`), which breaks on variants
("Takamol" vs "Takamol for Business Services"). Now the client card joins finance by the confirmed
link (resolving the app's legacy record id → the businesses uuid via `window.__bizUuid`), and only
falls back to exact-name when a client has no link yet.

**App (all live, self-contained layers):**
- `finLoad` also loads `finance_client_links` → `FIN.linkByGroup` / `FIN.groupsByBiz`.
- Client card finance section: "Linked to Direct finance · N invoices" (by ID) or "matched by name —
  not linked yet; link it now" (fallback, clickable → opens the map).
- **v53 mapping tool** — "🔗 Link finance to clients" button on Finance + the client-card nudge open a
  modal listing every invoice group (revenue + count + ✓/⚠) with a picker → client / individuals /
  unmapped. Saves to finance_client_links (admin/manager). Exposed `window.fc/canFinEdit/canFinView`.

**The remaining manual step for the team:** open Finance → "Link finance to clients" and map the 6
flagged groups (Ma'aden (merged), Riyadh Chamber (merged), MDD (merged), Abdel Hadi … (merged),
Special Forces …, STUDY ABROAD COMMISSION) to the right client — or mark individuals. Do NOT auto-map
these (no-cross-company-smuggling rule); the team confirms.

Verified EN + AR in the harness (linked vs unlinked, mapping save, Direct-ID load, regression + users
+ RTL), 0 page-errors. Commits `d45cd2c … c6deccd`.

---

## 2026-08-10 (evening, +) · Finance reports rolled up by the LINKED (canonical) client

Closes the last gap in the spine. Finance reports used to group by the raw invoice `client_group`
name, so two spellings of one company ("Ma'aden" + "Maaden Co") would show as two rows even after the
team links them. Now they roll up by the **linked client**.

**What changed (all in the main finance module of `index.html`, not a new layer):**
- New `finCanon(client_group)` helper: resolves each finance group → its canonical client via
  `finance_client_links` (the confirmed link, business_id → client name through `window.__bizUuid`), or
  a single "Individuals / not a client" bucket, or (if not linked yet) its own raw name. Memoised per
  render (`clearFinCanon`).
- **Report Builder** now has a **"Client (linked)"** group-by dimension and defaults to it. Two groups
  mapped to one client collapse into that client's single line. "Invoice group (raw)" is still there in
  the dropdown to see the underlying groups. A one-line legend shows how many groups aren't linked yet
  (⚠ + "Link them" → opens the map) so the total is never silently incomplete.
- **Overview → Top clients** rolls up the same way; clicking a client drills into the **Ledger**
  filtered to that client (all its groups) via a removable chip. The Ledger itself stays invoice-level
  on the raw group (it's a list, not a rollup).

**No money changes.** Grand totals are identical to before (proved: linked-grouping total == raw-grouping
total). On the live data today it looks almost the same — each of the 6 mapped clients still maps 1:1, so
only the 2 "individuals" groups now sit together in one line. The collapse activates automatically the
moment the team links a duplicate spelling. Real-data landmine: 0 links to a missing business, 0 links to
a non-client, 0 clients with multiple groups (yet).

**Bonus fix found along the way:** `isArF()` (the finance module's Arabic check) was calling an `isAr`
that isn't in its scope, so it **always returned false** — the entire Finance page (KPI cards, income-by-
service, AR aging, top clients, report builder) was rendering its labels in **English even in Arabic
mode**; the Arabic you saw came only from a separate relabel layer. Made `isArF()` read `LANG` directly.
Finance is now fully Arabic in AR mode (verified by screenshot, EN + AR).

Verified: new `test-rollup` (14/14, collapse + grand-total invariant + legend + drill + EN/AR), full
existing suite (test-final/link/map/did/ar/users) all green, 0 page-errors; real-Supabase landmine clean.

---

## 2026-08-10 · deep button sweep (inside cards + Settings tidy)

Follow-up to the page sweep. The first sweep only clicked buttons on the **main** list pages;
this pass went **inside the detail cards, modals, and Finance sub-tabs**, and finished the
Arabic + declutter on **Settings**. All verified in the harness (EN + AR), **0 page-errors**.

1. **"Open in Finance ledger ↗" (lead & client detail) — was a dead button, now FIXED.** The
   onclick built `FIN.f.client=` from `JSON.stringify(name)`, whose double-quotes closed the
   HTML `onclick="…"` attribute early → the browser saw broken code and the button threw
   "Unexpected end of input" and did nothing. Fix: HTML-encode the quotes
   (`JSON.stringify(cg).replace(/"/g,'&quot;')`) so the attribute stays whole. (index.html ~9769)
2. **"Convert to client" confirm — was half-English in Arabic, now FIXED.** The confirm text
   was English-only, so the friendly-confirm wrapper showed an Arabic title over an English
   body. `convertToClient` now builds the whole message bilingually. (index.html ~1007)
3. **Finance → Import — softened.** Clicking "Check file" with no file chose an English
   blocking `alert`; now a bilingual toast. The Import panel's visible labels
   ("Import invoices (CSV)", "Check file", the restricted note) are bilingual. (index.html ~9067)
4. **Settings tidy — new `v50` layer at end of file.** The clean "Daily use" tiles + "Advanced"
   drawer are fine; the problem was a stack of **older cards leaking in below them**, some pure
   developer/audit read-outs, and in Arabic several stayed English. `v50` (runs only on Settings,
   after the late cards inject):
   - **Hides** two dead cards: **"Security & integrity"** (ZATCA hash / XSS / PII audit — a
     developer check, per CLAUDE.md rule #5 not something the team should see) and **"Generator
     templates"** (its re-learn button already lives under Advanced; the rest was a dev "show
     tokens"). The real proposal **Generators** card stays.
   - **Finishes the Arabic** on the remaining cards (Generators, Backup & restore rows/buttons,
     Backup destination, the Admin card's buttons + team line) via a **text-node walk** so it
     never touches an icon or the file input inside "Import JSON". Settings now reads all-Arabic.
   - Reversible: it only sets `display`/`textContent`, adds no data. To undo, delete the `v50` block.

Regression: `test-final` (nav/back/filters/RTL) and `test-users` (Team & Access roles, enforcement,
RTL) both green, 0 errors. Settings went from 10 visible cards → **8**, jargon markers → none.

---

## 2026-08-10 (later) · Settings feature audit + brand design pass

**Settings audit.** Opened and used every Settings feature against real Supabase data. Verdict:
almost everything is real business tooling (proposal generators, About/Tender one-pagers with the
real CR/VAT/IATA, Commercial Credit Pool, Activity&Audit, Archive, honest Connections/Sync page).
The only true leftovers were developer cards (already suppressed by v50).

**Backup removed entirely (owner's call).** The manual backup / Export-Import JSON apparatus is gone
— Settings cards, the Today "Backup destination" banner, the daily tile, and the Advanced "Export
full state JSON". Hidden globally via one CSS rule in the `v51` style block
(`.v24BkBanner,.v24BkCard,#v21SettingsBackup{display:none!important}`) plus removal from the v26
daily-tiles / advanced arrays. Rationale: GitHub + Vercel + Supabase own code/data safety, so a
manual JSON backup isn't a team tool (and Import could overwrite everything). Settings = 6 real cards.

**Brand design pass (colors were already exact; this added character).**
- Login: the Direct brand voice line + a Direct favicon (was none).
- `v51` layer: a Direct-gradient **brand band** on the Today dashboard (the promise + date), and a
  slim orange→gold **accent line under the top bar** on every page.
- `v52` layer + `tile()` edit: a Direct orange **left-accent bar** on KPI stat cards (both the
  inline `tile()` KPIs and the `.kpi` cards, via `border-inline-start` so it flips in Arabic RTL),
  and a short **underline accent** under each page's main title.
- Licensed brand fonts (Proxima Nova / Zarid Slab) were deliberately NOT embedded — the repo is
  public, so shipping the font files would redistribute licensed assets. Font stack stays Cairo/Inter.

All verified EN + AR (RTL correct), 0 page-errors. Shipped live in several small commits
(`d3cf06c` … `84ab099`). Layers v51/v52 are self-contained and reversible (display/textContent only).

---

## Shipped live 2026-08-09 (evening), production HEAD `c7aeba4`

Five fixes from the user testing on his phone, all deployed to `directksab2b.com`:

1. **Login "Database error querying schema" — FIXED (auth DB).** `test@directksa.com`
   had NULL token columns in `auth.users` (the GoTrue NULL-token bug → 500 on password
   sign-in). Fix run via SQL: `UPDATE auth.users SET confirmation_token=COALESCE(...,''),
   recovery_token=…, email_change_token_new/current=…, phone_change_token=…,
   reauthentication_token=…` for any NULL. 0 NULL rows remain. The 3 real accounts were
   never affected. **If a new account ever can't sign in with that same error, re-run it.**
2. **Login form flashed on reload — FIXED.** The cloud-layer overlay now shows a neutral
   loading **splash** first and only swaps to the sign-in form when `getSession()` returns
   no session (see `showLoginForm()`/`formShown` near the login overlay). Login inputs → 16px
   (stops iOS zoom).
3. **Mobile "page is off" — FIXED (`v47`… actually `v46-mobile` block).** Root cause was the
   **top bar**, not the tables: older layers forced `.top .tools{width:100%;margin-left:auto}`
   so the tools overflowed and dragged the page wide (zoomed-out). The `v46` `@media(max-width:640px)`
   block now forces a clean wrapping row + hides desktop-only buttons on phones. Tables already
   scroll inside `.tbl-wrap`. 0 horizontal overflow on every page.
4. **Back button ejected you out of Leads — FIXED.** The clean-URL router now records the
   active Leads/Clients **filters + sub-tabs** in `history.state` (snapFilters/restoreFilters),
   and wraps `render`/`renderLeads`/`drawLeads`. Back peels the last filter and keeps you on the
   page; only leaves once the page is at its default. Search text is deliberately excluded.
5. **Declutter (`v47-declutter` CSS block + 2 small edits) — the user's big ask.** Hides the
   explanatory noise on every page: page subtitles (`#vSub`, `.v26_3-section-head .sub`), the
   "?" help circles (`#v26TipBtn`, `.help-dot`), the "N worked leads with no owner · e.g. …"
   banner (`#v39strip`), the grey card helper lines (`.card .ch-sub`), and the broken duplicate
   Clients KPI bars (`#cl_dash`). Also removed the Today keyboard-shortcut footer (template).
   **All reversible** — delete the `v47-declutter` `<style>` to bring descriptions back. The
   user wants longer explanations in a **separate blueprint doc**, not on the working screens
   (a likely next task).

Harness note: `scripts/qa/mock-supabase.mjs` now serves `index.html` for deep paths (SPA
fallback, mirrors `vercel.json`) so `/leads` etc. and reloads work in tests.

### Login bug — the COMPLETE fix (important)
GoTrue reads EIGHT `auth.users` columns as required strings; ANY NULL among them throws
"Database error querying schema" on password sign-in. The first pass only fixed the 6 token
columns, so the error just moved to `email_change` (then it would've moved again). The full
set that must be `''` (never NULL): `confirmation_token, recovery_token, email_change_token_new,
email_change_token_current, phone_change_token, reauthentication_token, email_change, phone_change`.
All are now `''` for every user (0 NULLs); `phone` staying NULL is fine (GoTrue treats it as
optional). **If this error EVER reappears for any account, run:** `UPDATE auth.users SET
<each of the 8> = COALESCE(<col>,'') WHERE <any> IS NULL;` I could not `ALTER ... SET DEFAULT ''`
(needs table owner), but new users made through the app's Team page go via GoTrue
`admin.createUser`, which sets `''` correctly — so this only bites manually-inserted users.

### 6 employee logins created (2026-08-09) + Team & Access page
Created via SQL (auth.users + auth.identities + app_users + access_allowlist), all with `''`
tokens (verified `password_ok=true`, so they authenticate), `must_change_password=true`,
role `team_member`, default `allowed_pages=['today','leads','clients']`:
assem.alsweed@directksa.com, mohammed.altuwaijri@directksa.com, raad.elkhair@directksa.com,
kareem.medhat@directksa.com, ahmed.aboelmagd@directksa.net, abdulaziz.alreshody@directksa.com.
(Temp passwords were shared privately with Abdulrahman, NOT committed — repo is public.)

**New `v48` "Team & Access" page** (Settings card + the top-bar Team/Access buttons repoint to it):
one panel to add a teammate, set role, switch on/off, reset password, and tick which pages each
person can open. Uses the existing `admin-users` edge function + `app_users.allowed_pages` +
the `v41` enforcement (`applyGate`/`myPages`). **Added `finance` to the `PAGES` list** (line
~8670) — it was missing, so Finance couldn't be gated before; now "Finance only" and
"hide Finance" work. Exposed `window.PAGES` + `window.applyGate`. Enforcement runs on a 2s
interval (hides disallowed nav, bounces off disallowed pages); Today is always allowed.

---

## 0. THE ONE LESSON THAT MATTERS (read first)

**The QA harness (`scripts/qa/mock-supabase.mjs`) serves FAKE data** — 60 rows named
"Test Company N", one funnel called "Default". It does **not** look like the real app.
Verifying UI against it will pass while the real screen is a mess. This burned a whole
sub-session: the "bulletproofing" was green while the user's live Leads page was full of
junk the harness never showed.

**To see what the user sees, do ONE of these before judging any UI:**
1. Pull real rows from Supabase (read-only) and feed them into a harness variant, **or**
2. Ask the user for a screenshot (the sandbox cannot reach `directksab2b.com` or
   `*.supabase.co`; images read perfectly).

Never claim a page is "clean/verified" from the mock harness alone again.

---

## 1. What this is + how it deploys

- One single-file app: `index.html` (~10k lines, vanilla JS, layered `<script>` blocks
  appended at the end — v18…v39). No build step.
- **Deploy:** push to production branch **`claude/new-session-9fhlp1`** → Vercel builds →
  live on **directksab2b.com** in ~30s. Dev branch: **`claude/content-audit-pages-f4lenf`**.
  Both are kept at the same commit. Open PR: **#12** (draft, into `main`).
- **Current production HEAD:** `1043570` (as of this brief).
- **Supabase project:** `vkxoeeoauexyfpzqufqd` (`direct-business`). `businesses` is a real
  table (safe, row-by-row). Read-only SQL via the Supabase MCP works from here.

---

## 2. Live DATA state (the important part)

Verified 2026-08-09:

| | count |
|---|---|
| Active businesses (not archived) | **1014** |
| — Active **leads** (`is_client=false`) | **969** |
| — Active **clients** (`is_client=true`) | **45** |
| Archived | 21 |
| Half-converted (column vs `raw` mismatch) | **0** (clean) |
| **BNPL/merchant-directory junk still in leads** | **201** |

### Client moves made this session (both reversible)
1. **Batch 1 (6):** companies with matched invoices → clients. Backup table
   `businesses_snapshot_20260809_clientmove`.
2. **Batch 2 (39):** companies from the **`Old Customers` + `Invoice history`** import that
   carry real billing (`total_sar>0`) → clients. Backup table
   `businesses_snapshot_20260809_clientmove2`. (Kayan Orphans 1.15M, Maaden 705k, Abdel
   Hadi Al-Qahtani 342k, Booking & Ticket 139k, + 35 more.)

All moves set `is_client=true` + `raw.isClient='true'` + `stage='won'` + `converted_date`.
**Undo any move:** `UPDATE businesses b SET is_client=s.is_client, stage=s.stage,
converted_date=s.converted_date, raw=s.raw FROM <snapshot> s WHERE b.id=s.id;`
Older full snapshots also exist: `businesses_snapshot_20260808` (pre-reset, 1035 rows).

---

## 3. ✅ DONE — the 201 BNPL/merchant-directory junk archived out of Leads

**201 leads were scraped retail brands** from source `BNPL / Fintech merchant directory` —
Samsung, IKEA, Amazon, Carrefour, PUMA, Victoria's Secret, Lacoste, Jarir, etc. (zero
billing, no owner, all dumped into `Partners & Tenders`). **On the user's "archive them"
(2026-08-09) they were archived** (`archived_at=now()`, NOT deleted). Active leads went
**969 → 768**. Backup: `businesses_snapshot_20260809_bnpl_archive` (1035 rows).
**Undo:** `UPDATE businesses b SET archived_at=s.archived_at
FROM businesses_snapshot_20260809_bnpl_archive s WHERE b.id=s.id AND b.source ILIKE 'BNPL%';`

Still open (small): **23 leads** carry a verbose flag
`confirmation_reason = "Client record is four months stale — …"` that clutters the card —
trim/soften it (not yet done; the user only confirmed the archive).

---

## 4. Leads source & funnel map (live)

**Sources** (active count): Travel Agencies 522 · **BNPL / Fintech merchant directory 200
(JUNK, see §3)** · Website Contact Form 120 · Contact form 48 · Service Integration
Partners 37 · Old Customers 36 · Invoice history 11 · Individuals from B2B 12 ·
Conferences 7 · Tender 7 · Direct outreach 5 · misc.

**7 funnels** (real, from the `funnels` table): Inbound · Outreach & Network · Travel Trade
· Partners & Tenders · Website Form — B2B · Website Form — Entities · Past Invoices.

**Who is / isn't a client:**
- **Are clients** (moved): `Old Customers` + `Invoice history` with billing = past customers.
- **NOT clients, correctly left as leads:** the big website-form names — **SAMA, NEOM,
  Alshaya, alfanar, Kemya/SABIC, Savvy Games, Webook** — came from `Website Contact Form`
  (inbound inquiries), have **no invoices**, and were never flagged. Do **not** move them
  without proof.
- **29 leads still show an amount but were left as leads on purpose** — they are tender
  values or deal estimates, not billing: tenders (Riyadh Chamber, Mawani, Red Crescent,
  IMSIU), **suppliers** (Amadeus Saudi Arabia, Booking.com), individuals, and junk ("A",
  "COM").

**The corporate portal "Companies" export is TEST DATA.** The user shared
Google-Drive exports of `Direct Corporate – B2B Admin Panel` (Companies/Invoicing/
Transactions/Team). The `Companies.html` there is only TEST companies (TEST NEW COMPANY,
Test Raad…) because that portal (corporate.directksa.com) **isn't launched**. It is **not**
the client list. The real client master lives in **Direct Payments** (payments.directksa.com,
~42 corporate clients) — still not exported to us. Getting that export is the real unlock
for client/finance accuracy.

**Known duplicates to dedupe later:** Ma'aden ×2, Abdel Hadi Al-Qahtani ×2–3, Booking
"&"/"and" Ticket Agency, Riyadh Chamber ×3, Directorate of Public Security (one archived
`is_client` shell).

---

## 5. Code shipped this session (all live)

- **v32** Finance "Income by service line" (gross = cost + service fee; service fee =
  taxable income). Read-only, no values changed.
- **v33** Leads "Service fit" map (per-service buys-elsewhere / with-Direct / not-a-fit).
  **v39 made it a collapsed one-liner that expands on tap.**
- **v34** Won→Client link: captures the **Direct client ID** (the link key) + linked/
  not-linked banner on clients.
- **v35** Suggested next step per lead stage (calm one-liner, EN+AR).
- **v36** Client profile "managed in Direct": hides the loud duplicate "KSA onboarding"
  button; **v39 folded it into the v34 strip** so a client shows ONE Direct strip.
- **v39** Detail-card tidy: one Direct strip, collapsed service map, dropped duplicate
  "Lifetime billed" from Key facts, trimmed `.ch-sub` helper subtitles.

### ⚠️ Reverted this session — do NOT re-apply blindly
- **v37 + v38 were REMOVED** (commit `1043570`). They hid the "All funnels"/"All stages"
  dropdowns, the "Has app" filter, a duplicate "Needs attention", and a duplicate
  Chain-of-command button. The user said those were **good filters** — they were restored.
  If you re-declutter the Leads toolbar, confirm with him first and check against real data.

---

## 6. Still open / backlog (Leads-focused first, per the user's current scope)

1. **Archive the 201 BNPL junk leads** (on his "archive them"). Highest-impact declutter.
2. **Trim the 23 stale-flag texts.**
3. **Get the Direct Payments client list** (real ~42 clients) — the app can't see it; the
   corporate-portal export is test data. This unlocks correct client classification +
   finance reconciliation + settles any remaining "is X a client" question.
4. **Dedupe** the ~10 duplicate company rows.
5. **Ownership is free text** — 746 leads have no real owner; blocks "show me my leads".
   Needs `assigned_to`/`account_manager` linked to real `app_users`.
6. **Finance values need the user's review** before the team/board leans on them: overall
   ~89% margin, several service lines at 100% because `cost_sar=0`, some duplicate invoice
   numbers. **No finance values were changed** — they're the team's figures, flagged only.
7. Finance Month/Quarter/Year period rollups (parked, touches values under review).

---

## 7. How to work here safely (recap)

- **Verify against REAL data or a screenshot**, never the mock harness alone (§0).
- Data changes: **snapshot first**, change `is_client` AND `raw.isClient` together, keep a
  one-line undo. Live tool the team uses — reversible beats clever.
- Code changes: append a self-contained `try/catch` `<script>` layer at the end of
  `index.html`; don't restructure the middle. Deploy = push dev + fast-forward
  `claude/new-session-9fhlp1`; verify the Vercel deployment goes READY.
- One clear next step at a time; the user is not a developer and has hit many dead ends —
  bias to reducing his workload and keeping everything reversible.

---

## 8. Links & references

- App: https://www.directksab2b.com · PR: `abdoulmagd911/Direct-Business#12`
- Docs to read: `CLAUDE.md`, `docs/BACKLOG.md`, `docs/DIRECT_MASTER_BRIEF.md`,
  `docs/DIRECT_SYSTEMS_MAP.md`, `docs/ROLES_AND_ACCESS.md`, this file.
- Snapshots (admin/manager only): `businesses_snapshot_20260808`,
  `_20260809_clientmove` (batch 1), `_20260809_clientmove2` (batch 2).
- Drive (shared this morning, exports of the real systems — mostly test/HTML dumps):
  "Executive CRM Dashboard" + "Direct Corporate – B2B Admin Panel" folders.
